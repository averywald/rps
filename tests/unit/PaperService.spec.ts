import { assert } from 'chai'
// import { shallowMount } from '@vue/test-utils'
import PaperService from '@/services/paper.service';

// describe('HelloWorld.vue', () => {
//   it('renders props.msg when passed', () => {
//     const msg = 'new message'
//     const wrapper = shallowMount(HelloWorld, {
//       props: { msg }
//     })
//     expect(wrapper.text()).to.include(msg)
//   })
// })

describe('PaperService', () => {
    // it('gives correct outcomes for battle', () => {
    //     // let arsenal: keyof typeof Weapon;
    //     // for (let weapon in Array.from(arsenal)) {
    //     //     console.log(Weapon[weapon]);
    //     let protoAccess = Object.getPrototypeOf(PaperService);
        
    //     assert(protoAccess.battle(Weapon.ROCK, Weapon.ROCK) == Outcome.TIE);
    //     assert(protoAccess.battle(Weapon.ROCK, Weapon.PAPER) == Outcome.LOSS);
    //     assert(protoAccess.battle(Weapon.ROCK, Weapon.SCISSORS) == Outcome.WIN);
    // })
})
